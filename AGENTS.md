# Repository rules

- Use TypeScript for all source files.
- Use single quotes for strings.
- Keep imports sorted and grouped (enforced by ESLint).
- Prefer functional React components.
- Keep UI mobile-first and responsive.
- Run `npm run lint` and `npm run typecheck` before committing when dependencies are installed.
